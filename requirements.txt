# Core Logic
aiofiles>=23.2.0
aiohttp>=3.9.0
pysnmp>=4.4.12
pywin32>=306; platform_system=="Windows"

# PDF & Converters
pymupdf>=1.23.0
python-docx>=1.1.0
pillow>=10.1.0

# Network Discovery
zeroconf>=0.131.0

# GUI
# tkinter is usually built-in, but dependencies for Pillow might be needed
